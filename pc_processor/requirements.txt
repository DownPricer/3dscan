# Dependances optionnelles pour mesh + export site-ready (GLB)
# Installation : py -3 -m pip install -r requirements.txt
open3d>=0.18.0
trimesh>=4.0.0
numpy>=1.24.0
# Textures Meshroom EXR -> PNG (AliceVision Texturing)
OpenEXR>=3.2.0
Pillow>=10.0.0
